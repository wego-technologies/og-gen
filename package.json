{
  "name": "og-gen",
  "version": "0.0.1",
  "description": "OpenGraph image generator",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "canvas": "^2.8.0",
    "express": "^4.17.1"
  },
  "scripts": {
    "pre-build": "yum install libuuid-devel libmount-devel zlib && cp /lib64/{libuuid,libmount,libblkid,libz}.so.1 node_modules/canvas/build/Release/",
    "build": "npm run pre-build && node index.js"
  }
}